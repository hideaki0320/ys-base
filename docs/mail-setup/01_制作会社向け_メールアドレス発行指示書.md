# メールアドレス発行のお願い（ys-base@yscc1986.net）

- 依頼日: 2026年9月7日
- 依頼元: Y.S.C.C.横浜 YS-BASE 運営（担当: ＿＿＿＿ / 連絡先: 045-621-8760）
- 対象ドメイン: yscc1986.net
- 希望納期: ＿＿＿＿

---

## 1. 依頼内容

以下のメールアドレスを **受信・返信できる状態** で発行してください。

```
ys-base@yscc1986.net
```

用途: YS-BASE（天然芝サッカーコート予約サイト https://ys-base.yscc1986.net ）の問い合わせ窓口、および予約確認メールの差出人・返信先。

---

## 2. 現在の構成（2026-09-07 に DNS / whois を確認した事実）

ドメインの管理元とメールの受信先は別のサービスになっています。

| 役割 | サービス | 根拠 |
|---|---|---|
| ドメイン登録（レジストラ） | お名前.com（GMO） | whois |
| DNS 管理 | Oracle Cloud DNS（ns1〜ns4.p68.dns.oraclecloud.net） | NS レコード |
| Web サイト（www.yscc1986.net） | Oracle Cloud 上のサーバー（132.226.11.12） | A レコード |
| **メール受信（MX）** | **Google Workspace**（aspmx.l.google.com ほか） | MX レコード。Google / Cloudflare / 権威サーバーの 3 系統で一致 |
| SPF | `v=spf1 include:spf.no1-server28.com include:_spf.google.com -all` | TXT |
| DMARC | `v=DMARC1; p=none; sp=quarantine; rua=mailto:no1dmarcreport@no1-server28.com; ...` | TXT |

**メールは Google Workspace で受信しているため、アドレスの作成は Google Workspace 管理コンソール（admin.google.com）で行います。DNS 側の作業はありません。**

Google Workspace の管理者が貴社でない場合は、管理者（クラブ側で Google Workspace を契約している担当者）へ本書を転送してください。
どなたが管理者か不明な場合は、MX を Google に向けた際の担当者にご確認ください。

---

## 3. 作成方法（いずれか 1 つ。推奨は A）

| 方式 | 費用 | 複数人で受信 | 備考 |
|---|---|---|---|
| **A. Google グループ**（推奨） | 無料 | できる | 「共同トレイ」として複数担当者で対応可能 |
| B. 既存ユーザーのエイリアス | 無料 | 1 人のみ | 特定の 1 名の受信箱に届く |
| C. 新規ユーザーアカウント | 有料（1 ライセンス） | 1 人 | 独立した受信箱が必要な場合のみ |

### A. Google グループで作成する場合の設定

1. Google Workspace 管理コンソール → グループ → 「グループを作成」
2. グループのメールアドレス: `ys-base@yscc1986.net`
3. メンバー（受信担当者）: 下記を追加
   - ＿＿＿＿@＿＿＿＿
   - ＿＿＿＿@＿＿＿＿
4. **外部からのメールを受け取れるように設定してください**（重要）
   - 投稿できるユーザーを「組織外の全員（インターネット上の誰でも）」に設定
   - この設定がないと、一般の利用者からのメールが「配信不能」で跳ね返ります
5. メンバーへの配信を「メールごとに配信」に設定（ダイジェストにしない）

### B. エイリアスで作成する場合

1. 管理コンソール → ユーザー → 対象ユーザー → 「ユーザー情報」→「メールエイリアス」
2. `ys-base@yscc1986.net` を追加
3. 対象ユーザー: ＿＿＿＿@yscc1986.net

---

## 4. DNS の変更は不要です（変更しないでください）

送信側（Resend）の DNS レコードは **すでに設定済み** で、当方で利用中です。
今回の作業で **DNS は一切変更しないでください**。特に以下のレコードは削除・編集しないようお願いします。

| レコード | 名前 | 値 |
|---|---|---|
| TXT | `yscc1986.net` | `v=spf1 include:spf.no1-server28.com include:_spf.google.com -all` |
| TXT | `resend._domainkey.yscc1986.net` | `p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQCeotac...`（DKIM 公開鍵） |
| MX | `send.yscc1986.net` | `10 feedback-smtp.ap-northeast-1.amazonses.com` |
| TXT | `send.yscc1986.net` | `v=spf1 include:amazonses.com ~all` |
| TXT | `_dmarc.yscc1986.net` | `v=DMARC1; p=none; sp=quarantine; ...` |
| CNAME | `ys-base.yscc1986.net` | `92uiq256.up.railway.app`（サイト本体） |

---

## 5. 完了確認（作業後にお願いしたいこと）

1. 外部のメール（Gmail など、yscc1986.net 以外）から `ys-base@yscc1986.net` へテスト送信し、**受信担当者に届くこと** を確認
2. そのメールに返信し、外部側で **返信が受け取れること** を確認
3. 上記 2 点が確認できたら、下記へ完了連絡をお願いします
   - 連絡先: ＿＿＿＿（電話 045-621-8760）

---

## 6. 備考

- 送信（予約確認メール等）は当方のシステム（Resend）から `ys-base@yscc1986.net` を差出人として行います。貴社側で送信設定を行う必要はありません。
- 将来 `info@yscc1986.net` のように既存アドレスへ転送したい場合は、上記 A のメンバーに追加するだけで対応できます。
